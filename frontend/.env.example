# FlowLens AI Frontend Environment Variables Example
# In production, if served from the same domain as FastAPI, leave empty to use same-origin /api.
# If deploying frontend separately (e.g. Vercel, Netlify, Firebase), set to backend domain:
VITE_API_URL=https://6e0d64c63b4a25.lhr.life/api
